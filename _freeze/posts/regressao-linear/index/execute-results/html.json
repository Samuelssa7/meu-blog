{
  "hash": "022480461eef02bc092b26a8abf48312",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Regressão Linear: Relação entre Salário e Estudo\"\nauthor: \"Samuel Andrey\"\ndate: \"2025-11-21\"\ncategories: [python, data-science, statistics, plotnine, numpy]\nimage: \"image.jpg\"\n---\n\nVeja um gáfico linear analisando anos de estudo e faixa salarial. 📈\n\n---\n\n### 1. Carregamento das Bibliotecas e Dados\n\nNesta etapa, importamos as bibliotecas necessárias para manipulação de arrays (`numpy`), dados (`pandas`) e gráficos (`plotnine`). Também carregamos os dados de entrada (`X.txt`) e saída (`y.txt`).\n\n::: {#dfd4ae00 .cell execution_count=1}\n``` {.python .cell-code}\nimport numpy as np\nimport pandas as pd\nfrom plotnine import ggplot, aes, geom_point, geom_abline, ggsave\n\nvalores_x = np.loadtxt('X.txt')\nvalores_y = np.loadtxt('y.txt')\n```\n:::\n\n\n---\n\n### 2. Cálculo dos Coeficientes\nAqui preparamos a matriz adicionando uma coluna de \"1s\" (`para o intercepto`) e aplicamos a fórmula da Equação Normal para encontrar os melhores valores para theta. Ao final, separamos o intercepto (`a`) e a inclinação (`b`).\n\n::: {#ba106a49 .cell execution_count=2}\n``` {.python .cell-code}\nX_b = np.c_[np.ones((len(valores_x), 1)), valores_x]\ntheta_best = np.linalg.inv(X_b.T.dot(X_b)).dot(X_b.T).dot(valores_y)\n\na = theta_best[0]\nb = theta_best[1]\n```\n:::\n\n\n---\n\n### 3. Geração e Salvamento do Gráfico\nCom os coeficientes calculados, criamos o gráfico contendo os pontos originais e a reta de regressão (`em vermelho`). O comando final salva o resultado como uma imagem.\n\n::: {#cf5f2cbe .cell execution_count=3}\n``` {.python .cell-code}\ndf = {\"x\": valores_x, \"y\": valores_y}\n\nplot = (\n  ggplot(pd.DataFrame(df), aes(\"x\", \"y\"))\n  + geom_point()\n  + geom_abline(intercept=a, slope=b, color=\"red\")\n)\n\nplot.save(\"grafico.png\")\n```\n:::\n\n\n---\n\n### Código Completo e Resultado\nAbaixo, o código completo para execução e a imagem gerada:\n\n::: {#6984b8f5 .cell execution_count=4}\n``` {.python .cell-code}\nimport numpy as np\nimport pandas as pd\nfrom plotnine import ggplot, aes, geom_point, geom_abline, ggsave\n\nvalores_x = np.loadtxt('X.txt')\nvalores_y = np.loadtxt('y.txt')\n\nX_b = np.c_[np.ones((len(valores_x), 1)), valores_x]\ntheta_best = np.linalg.inv(X_b.T.dot(X_b)).dot(X_b.T).dot(valores_y)\n\na = theta_best[0]\nb = theta_best[1]\n\ndf = {\"x\": valores_x, \"y\": valores_y}\n\nplot = (\n  ggplot(pd.DataFrame(df), aes(\"x\", \"y\"))\n  + geom_point()\n  + geom_abline(intercept=a, slope=b, color=\"red\")\n)\n\nplot.save(\"grafico.png\")\n```\n:::\n\n\n![Gráfico de Regressão Linear](grafico.png){width=100%}\n\n",
    "supporting": [
      "index_files"
    ],
    "filters": [],
    "includes": {}
  }
}